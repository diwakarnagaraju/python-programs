# Write a Python program to calculate the area of arectangle given its length and width

base = float(input("enter traingle width: "))
height = float(input("Enter tiangle Height: "))
area = 0.5 * base * height
print(f"The Area of Triangle with {base} and {height} is {area}")
